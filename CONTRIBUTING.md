# 贡献指南

感谢参与“化安智控”。本项目包含化学品安全、设备巡检、事故应急和过程控制内容，修改时请同时重视可用性与安全边界。

## 本地开发

1. 安装 Node.js 22.13 或更高版本。
2. 运行 `npm ci` 安装锁定依赖。
3. 运行 `npm run dev` 启动开发环境。
4. 提交前运行 `npm test`。

## 提交要求

- 每个 Pull Request 聚焦一个主题，并清楚描述动机和验证结果。
- 不要提交 `node_modules`、构建目录、APK、签名文件、环境变量或本机 IDE 配置。
- 界面变化需兼顾桌面端和窄屏手机，重要操作应保留键盘与触控可达性。
- 新增或修改化学品、巡检、应急数据时，应说明权威来源、版本、适用范围和更新时间。
- 不得把教学模型或辅助判断表述为真实生产控制、法定检验或专业安全结论。

## 分支与提交信息

建议从 `main` 创建短生命周期分支，名称使用 `feature/...`、`fix/...` 或 `docs/...`。提交信息使用简短祈使句，例如：`fix: 修复手机端报告导出`。

## Android 构建

Android 调试包的环境和命令见 [docs/ANDROID_BUILD.md](docs/ANDROID_BUILD.md)。公开发布前必须使用正式签名，并完成真机、安全与合规测试。
